{% extends '::base.html.twig' %}

{% block body %}
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Date</th>
                <th>Visible pour les utilisateurs</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for election in elections %}
                <tr>
                    <td>{{ election.label }}</td>
                    <td>{{ election.performanceDate|date }}</td>
                    <td>
                        {% if election.active %}
                            <span class="glyphicon glyphicon-ok text-success"></span>
                        {% else %}
                            <span class="glyphicon glyphicon-remove text-danger"></span>
                        {% endif %}
                    </td>
                    <td>
                        <div class="btn-group btn-group-xs">
                            <a href="{{ path('election_edit', {'id': election.id}) }}" class="btn btn-info">
                                <span class="glyphicon glyphicon-pencil"></span>
                            </a>
                            <a href="{{ path('election_delete', {'id': election.id}) }}" class="btn btn-danger">
                                <span class="glyphicon glyphicon-trash"></span>
                            </a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}