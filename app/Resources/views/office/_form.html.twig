{{ form_row(form.name) }}
{{ form_row(form.regularOpeningHour) }}
{{ form_row(form.regularClosingHour) }}
{{ form_row(form.address) }}

<div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
        {{ form_rest(form) }}
        <input type="submit" class="btn btn-primary">
    </div>
</div>
